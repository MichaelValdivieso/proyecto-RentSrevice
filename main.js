
import { flat } from "./flat";



/*
let newProperty = new flat(city, street, number, size, ac, year, price, date);
let properties = JSON.parse(localStorage.getItem('propierties')) || [];
saveToLocalStorage = () => {
    localStorage.setItem('propierties', JSON.stringify(properties));
}
properties.push(newProperty);
saveToLocalStorage();
filterTable();


*/
